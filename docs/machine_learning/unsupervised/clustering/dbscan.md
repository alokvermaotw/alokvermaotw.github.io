- Density-based spatial clustering of applications with noise
- Can detect clusters that have unique, nonspherical, non-bell-like shapes. like below
![[dbscan_example.png]]
- doesn’t rely on bell curves, but rather relies on careful considerations of the distances between each of the points within and between clusters.
