- Mean Absolute Error
- Take the difference between actual and predicted --> Absolute it --> take the mean

# When to use
- if you want an error metric that treats all errors equal
- if you want to get an interpretable value.